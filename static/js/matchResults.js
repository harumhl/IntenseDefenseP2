/* matchResults.js */

matchResultsState = 
{
	preload: function()
	{

	},
	create: function()
	{
		
        // Present a winner.png and loser.png to the appropriate players
        
        // Store who won the game like player.win = 1; in server
        
		/*
        if (matchNum == 1) {
            ; // switch roles
         // idea: socket.send('switchRoles');
         in server - else if (text == 'switchRoles') {
           if (player.state == 'attacker') player.state = 1;
           if (player.state == 'defender') player.state = 2;
         
          if (player.state == 1) player.state = 'defender';
          if (player.state == 2) player.state = 'attacker';
         }
        }
        else if (matchNum == 2) {
            matchNum = 0;
            game.state.start('endRound');
        }*/
	},
	update: function()
	{

	}
};